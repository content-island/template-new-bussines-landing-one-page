---
import Layout from '#layouts/Layout.astro';
import PrivacyPolicyComponent from '#pods/legal/PrivacyPolicy.astro';
import { getPrivacyPolicyMeta } from '#pods/legal';

const { title, description } = await getPrivacyPolicyMeta();
---

<Layout title={title} description={description}>
  <PrivacyPolicyComponent />
</Layout>
